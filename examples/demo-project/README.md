# Demo Project: 订单审批流 v1.0

This tiny Python project exists only to exercise the workflow. It models order
submission, approval, rejection, and the regression rule that an applicant cannot
approve their own order.

Run tests:

```powershell
python -m unittest discover -s examples/demo-project/tests
```
