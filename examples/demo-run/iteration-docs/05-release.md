# Release

## Release Content

- GitLab AI workflow project skeleton.
- Reusable templates.
- Local workflow installation.
- CI policy and smoke checks.
- Orchestrator skeleton.
- Demo project and demo-run artifacts.
- First-batch draft skills.

## Configuration

No production configuration changes. GitLab labels, milestone, issues, and notes
were created in the demo project.

## Database

No database changes.

## Rollback

Revert the MR.

## Manual Checks

- Protected branch is enabled.
- Successful pipeline is required before merge.
- Discussions must be resolved before merge.
- Runner availability must be confirmed. The first pending pipeline was caused
  by no active runner. Prefer Docker executor with `python:3.12-slim`.

## CI/CD Verification

- Pipeline #19841 succeeded.
- The pipeline showed `policy`, `validate`, `test`, `package`, and `release`
  stages.
- Artifacts were generated by `workflow_contract`, `package_open_source`, and
  `release_dry_run`.
