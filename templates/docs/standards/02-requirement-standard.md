# Requirement Standard

Requirements must include background, goal, user scenario, scope, non-goals,
permissions/data impact, external dependencies, acceptance criteria, priority,
and expected release timing.
