interface:
  display_name: "GJ Workflow Next"
  short_description: "Route new or existing GitLab work"
  default_prompt: "Use $gj-workflow-next to route this new requirement or inspect current workflow state and choose the next action."
